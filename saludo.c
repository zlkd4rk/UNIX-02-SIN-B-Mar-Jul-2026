#include <stdio.h> //Start the code with a library, this include the Standar Input/Output library.
//
int main(){  //the principal function, and return a int number to comprobate the code run correctly 
    printf("Hello World!\n"); //The message is about to print in the terminal and use \n for a newline.

    return 0; //If return a 0, indicate the code runs correctly with no errors.



 } //finsih of the main function 